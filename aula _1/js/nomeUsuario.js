const nome = window.prompt("Digite seu nome: ");

const curso = window.prompt("Curso escolhido: ");

window.alert(nome + " " + curso);

var nomeCompleto = "tsetste";
// array
var carro = [];

// numero

var idade = 25;

//BOOLEAN
var ligado = true;